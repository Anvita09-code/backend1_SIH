from dataclasses import dataclass

@dataclass(frozen=True)
class CalibrationRegistry:
    """
    Centralized calibration parameter registry for Member 1 OTE analytics.
    Phase 1 Foundation: Empty typed registry to be populated in future phases.
    """
    pass
